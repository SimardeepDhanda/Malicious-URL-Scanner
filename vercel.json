{
  "version": 2,
  "installCommand": "npm install && cd backend && npm install",
  "buildCommand": "",
  "functions": {
    "api/index.ts": {
      "includeFiles": "backend/**"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index"
    }
  ]
}
